#!/usr/bin/make -f

DH_VERBOSE=1

BUILD_FLAGS = -j8

#DEB_BUILD_OPTIONS = "parallel=8"
#DH_OPTIONS = "--parallel"

clean:
	dh_testdir
	dh_auto_clean
	rm -rf debian/tmp
	rm -rf debian/liboce*.[sd]*
	rm -rf debian/liboce
	rm -rf debian/liboce-dev

%:
	dh $@

.PHONY: clean build 
